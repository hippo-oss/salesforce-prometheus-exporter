version: '2.1'

defaults: &defaults
  working_directory: ~/project
  machine: true

jobs:
  build:
    <<: *defaults
    steps:
      - checkout
      - run:
          name: Building Docker Image.
          command: |
            docker build -t salesforce-prometheus-exporter .

workflows:
  build-and-release:
    jobs:
      - build
